# Disabled — Cloudflare Pages handles CI/CD directly from GitHub.
name: Deploy Next.js site to Pages (disabled)

on:
  workflow_dispatch:
    inputs:
      disabled:
        description: 'This workflow is disabled. Deploy via Cloudflare Pages.'
        required: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo disabled
